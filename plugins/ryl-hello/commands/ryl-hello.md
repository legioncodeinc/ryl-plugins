---
description: Confirm the ryl plugin pipeline is alive
---

Run the ryl-hello skill check: confirm custom ryl plugins load correctly and report the plugin pipeline status (author in our-plugins/, install into glm/packages/, load at startup).
